{
  "name": "necktie-website",
  "version": "0.6.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node site.mjs dev",
    "build": "node site.mjs build",
    "start": "node site.mjs start",
    "test": "npm run build && node --test tests/rendered-html.test.mjs",
    "lint": "node --check site.mjs && node --check content.mjs && node --check tests/rendered-html.test.mjs"
  },
  "type": "module"
}

{
  "name": "necktie-mcp",
  "version": "0.3.0",
  "description": "Optional MCP fallback that serves Necktie Core as a prompt and read-only tool.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test ./test/*.test.js"
  },
  "dependencies": {
    "@gillcash/necktie": "file:..",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  }
}
